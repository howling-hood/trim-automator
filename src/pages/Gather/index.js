import { Typography } from "@mui/material";
import React from "react";

const Gather = () => {
  return (
    <Typography
      variant="h4"
      color="primary">
      Gather
    </Typography>
  );
};
export default Gather;
